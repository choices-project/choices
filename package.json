{
  "name": "choices-repo",
  "private": true,
  "devDependencies": {
    "husky": "9.1.7"
  },
  "scripts": {
    "prepare": "husky install",
    "ci:install": "npm ci --ignore-scripts",
    "ci:verify": "npm audit --audit-level=moderate"
  }
}
