version: 2
updates:
  # Disabled: Only update dependencies for critical security vulnerabilities manually
  # Dependabot PRs are disabled to maintain stability with verified, tried-and-true versions
  # Security alerts will still be available via GitHub's security tab
  # - package-ecosystem: npm
  #   directory: /web
  #   schedule: { interval: weekly }
  #   open-pull-requests-limit: 0

  # GitHub Actions: Only update for critical security issues
  - package-ecosystem: github-actions
    directory: /
    schedule: { interval: monthly }
    open-pull-requests-limit: 2
    # Only create PRs for security updates - manual review required