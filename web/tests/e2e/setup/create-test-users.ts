/**
 * Modern Playwright tests rely on in-memory fixtures rather than seeded users.
 * Keep this script as a no-op so existing npm workflows remain compatible.
 */

console.log('🧪 E2E setup: no Supabase seeding required for the current test suite.');
console.log('    Tests create deterministic in-browser fixtures instead.\n');

