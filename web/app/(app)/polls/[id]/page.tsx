/**
 * App Route - Re-exports from canonical features implementation
 * 
 * This ensures the app route uses the SSR-safe canonical implementation
 * while maintaining the expected URL structure.
 */

export { default } from '@/features/polls/pages/[id]/page';