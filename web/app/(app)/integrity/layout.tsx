export const metadata = {
  title: 'Integrity Dashboard | Choices',
  description: 'Monitor platform integrity, transparency reports, and democratic safeguards.',
};

export default function IntegrityLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
