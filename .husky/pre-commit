#!/usr/bin/env bash
set -euo pipefail

# Block new legacy files
if git diff --cached --name-only --diff-filter=A | grep -E '^web/components/(polls|voting|admin/layout)/'; then
  echo "❌ New legacy files under web/components/* blocked. Use canonical paths."; exit 1;
fi

# Type & lint in UTC
export TZ=UTC
cd web && npm run types:strict
cd web && npm run lint